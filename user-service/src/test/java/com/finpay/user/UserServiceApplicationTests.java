package com.finpay.user;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Smoke test asserting that the user-service Spring application context starts.
 */
@SpringBootTest
class UserServiceApplicationTests {

    @Test
    void contextLoads() {
        // Fails the build if component scanning, configuration or the classpath is broken.
    }
}
