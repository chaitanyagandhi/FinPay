package com.finpay.registry;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Smoke test asserting that the service-registry Spring application context starts.
 */
@SpringBootTest
class ServiceRegistryApplicationTests {

    @Test
    void contextLoads() {
        // Fails the build if component scanning, configuration or the classpath is broken.
    }
}
