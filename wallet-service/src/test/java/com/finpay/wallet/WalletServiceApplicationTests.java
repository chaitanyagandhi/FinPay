package com.finpay.wallet;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Smoke test asserting that the wallet-service Spring application context starts.
 */
@SpringBootTest
class WalletServiceApplicationTests {

    @Test
    void contextLoads() {
        // Fails the build if component scanning, configuration or the classpath is broken.
    }
}
