package com.finpay.gateway;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Smoke test asserting that the api-gateway Spring application context starts.
 */
@SpringBootTest
class ApiGatewayApplicationTests {

    @Test
    void contextLoads() {
        // Fails the build if component scanning, configuration or the classpath is broken.
    }
}
