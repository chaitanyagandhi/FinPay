package com.finpay.payment;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Smoke test asserting that the payment-service Spring application context starts.
 */
@SpringBootTest
class PaymentServiceApplicationTests {

    @Test
    void contextLoads() {
        // Fails the build if component scanning, configuration or the classpath is broken.
    }
}
